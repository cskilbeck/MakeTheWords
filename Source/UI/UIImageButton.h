//////////////////////////////////////////////////////////////////////

#pragma  once

//////////////////////////////////////////////////////////////////////

namespace UI
{
	//////////////////////////////////////////////////////////////////////

	struct ImageButton : Image, Button
	{
		//////////////////////////////////////////////////////////////////////

		ImageButton(Texture *texture, Vec2 pos, Vec2 size, Point2D uvtl, Point2D uvbr)
			: Image(texture, pos, size, uvtl, uvbr)
			, Button()
		{
		}

		//////////////////////////////////////////////////////////////////////

		ImageButton(Texture *texture, Vec2 pos, Vec2 size)
			: Image(texture, pos, size)
			, Button()
		{
		}

		//////////////////////////////////////////////////////////////////////

		ImageButton(Texture *texture, Vec2 pos)
			: Image(texture, pos)
			, Button()
		{
		}

		//////////////////////////////////////////////////////////////////////

	};

//////////////////////////////////////////////////////////////////////

} //::UI
